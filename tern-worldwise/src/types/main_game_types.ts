export enum GameState {
    LOADING,
    MENU,
    ROUND,
    RESULT,
}

export enum RoundState {
    QUESTION,
    RESOLUTION,
    FLIGHT,
    NUGGET
}

export enum GlobeState {
    LOADING,
    READY,
    AUTO_MOVING,
    LOCKED,
}